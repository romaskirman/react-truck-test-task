export interface Truck {
    id: string;
    name: string;
    maxWeight: number;
    currentWeight: number;
}

export interface Product {
    id: string;
    name: string;
    imageUrl: string;
    price: number;
    weight: number;
}